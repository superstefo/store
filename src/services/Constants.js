let ipAdress = 'http://localhost:5000/';

const Const = {
  URLS: {
    BASE_URL: ipAdress,
    ORDER:  ipAdress + "order",
    AUTH: ipAdress + "auth"
  },
  AUTH_HEADER_NAME: 'authsecret',
  VERSION: '0.10'
}

export default Const;
